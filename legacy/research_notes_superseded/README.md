# `legacy/research_notes_superseded/` — archived `ops/research_notes/` (2026-08-10)

54 files moved here from `ops/research_notes/` during a doc-hygiene pass. Nothing
was deleted. Same method and same caveat as `legacy/status_superseded/README.md`.

Note specifically: many of these are cited by `brief_path` in
`status/RESEARCHER_REPORTS.jsonl`, which is a git-tracked live ledger. Those
pointers now resolve here rather than to `ops/research_notes/`. The files were
archived rather than deleted precisely so those `brief_path` fields do not dangle.

Recover with `git mv legacy/research_notes_superseded/<name> ops/research_notes/`.
