# Hunyuan-A13B keep13 + fresh NTP：现状、根因与下一步

> 更新日期：2026-07-14  
> 本文汇总 Hunyuan-A13B 的语义层定位、`keep13+fresh2/4` 架构实验、GPU/FSDP bug、性能瓶颈、loss 异常，以及它与 Qwen3/Hy3 实验的真实可比性。

## 1. 目标

当前目标是验证一种极简语言模型架构：

1. 找到语义处理截止层 `j`；
2. 保留预训练模型前 `j` 层；
3. 删除原来的上层；
4. 添加 2 层或 4 层随机初始化的 next-token prediction（NTP）模块；
5. 在 SlimPajama 上继续预训练，先验证 20k steps，再决定是否扩展。

对 Hunyuan-A13B，QCMem depth-partition j-sweep 得到：

```text
L = 32
j = 13
j / L = 0.406
```

因此当前两条候选架构为：

```text
keep13 + fresh2 = 15 layers，约 37.963B 总参数
keep13 + fresh4 = 17 layers，约 42.955B 总参数
```

这里的 A13B 表示每个 token 约激活 13B 参数，不代表模型只存储或只通信 13B 参数。

## 2. `j=13` 到底证明了什么

`j=13` 来自 QCMem 实验。QCMem 的实际执行路径是：

```text
原始层 0–12
    ↓
缓存 h13
    ↓
原始层 13–31
    ↓
原始 final norm / lm_head
```

它证明的是：

> 第 13 层已经形成足够丰富、适合缓存和重新组合的语义表示。

它没有直接证明：

> 删除原来的 19 个上层后，只接 2/4 个随机层就能马上成为正常语言模型。

`keep13+fresh2/4` 是比 QCMem 更强、更激进的假设：

```text
原始层 0–12
    ↓
删除原始层 13–31
    ↓
2/4 个随机初始化 MoE 层
    ↓
原始 final norm / lm_head
```

## 3. 与 Qwen3、Hy3 的真实关系

### 3.1 Qwen3-8B

Qwen3-8B 是 dense 模型。仓库中真正跑通、能直接类比 A13B 的大模型实验只有这一条：

- 原模型 36 层；
- `keep12+fresh2`，截为 14 层、3.95B 参数；
- 全参数 healing，fresh LR `1e-4`，inherited LR `2e-5`；
- 20,000 optimizer steps；
- effective batch 128，约 5.24B tokens；
- step 20000 PPL 14.42，末 10 点均值 PPL 14.495；
- DONE、无 NaN、有 final checkpoint。

用户口头状态是 Qwen 已到 40k，但当前仓库可核验的日志和 checkpoint 止于 20k；200k 仍是续训计划。

Qwen 还完成过 QCMem j12 + LoRA 自蒸馏 1k/4k steps，但该实验保留原来的上 24 层，不等于截断加 fresh 层。

### 3.2 Hy3-80L

内部 Hy3 是 80 层、597GB、192 experts、top-8 的 MoE 模型。

它完成过：

- QCMem j-sweep，确定 `j≈32/80`；这是评测，不是训练；
- 完整 80 层上的 QCMem j32 + LoRA r32/α64 自蒸馏 4k steps；
- 蒸馏后 16k PPL gap 从 1.496 降到 1.078，top1 从 0.773 升到 0.871。

但 Hy3 没有可核验的 `keep32+fresh2/4` 成功训练记录。

仓库中的 `train_hyv3_probe2.py` 对应另一个 48 层 Hy-MT2-30B-A3B，计划 `keep36+fresh2` 跑 200-step smoke；当前没有可靠成功 step 日志或 checkpoint，不能作为速度或收敛基线。

因此，严谨结论是：

> 大模型 keep+fresh 的直接成功证据只有 Qwen3；Hy3 只为 `j≈0.4L` 和“保留原上层时 LoRA 能修复缓存读出”提供旁证。

## 4. A13B 已完成实验

| 配置 | 结果 | 速度 | 显存 | 结论 |
|---|---|---:|---:|---|
| keep13+fresh2，7 GPU，gaccum1 | step1 loss 100.3894，gnorm 1286.64 | 18.98s/step | 107.2GB | 完整训练 step 成功 |
| keep13+fresh2，8 GPU，gaccum2 | loss 85.9229 → 90.2023 | 42.52 → 87.62s/step | 93.8 → 101.5GB | FSDP accumulation 修复通过 |
| keep13+fresh4，8 GPU，gaccum2 | 6 steps；103.7734, 93.9581, 92.5722, 97.0448, 82.8790, 89.7127 | 稳态约 123s/step | max allocated 111.4GB；reserved 接近 143GB | 用户要求暂停；质量尚无结论 |

fresh4 已于 step 6 暂停。所有训练进程已退出，8 张 H200 回到 1MiB、0% 利用率，无新增 Xid。当前没有活跃 A13B 训练。

## 5. 已修复的执行 bug

### 5.1 FSDP + gradient accumulation 卡死

最初训练卡住的根因是 `FULL_SHARD` 下使用 `model.no_sync()`：

- `no_sync()` 会保留完整、未分片的梯度；
- A13B 截断模型仍有约 38–43B 总参数；
- gradient accumulation 第一个 microbatch 后显存接近耗尽；
- 后续 rank 在 CUDA/NCCL 中互相等待，看起来像训练 loop 死锁。

修复方式是取消 FSDP `no_sync()`，每个 microbatch 都执行 reduce-scatter，同时在本地 gradient shard 上累积。修复后 forward、backward、clip grad 和 optimizer step 均已通过。

### 5.2 诊断结束后 checkpoint 汇聚过重

旧 smoke 在训练 step 成功后，会汇聚完整模型和 optimizer state，导致长时间停留在 final save。

已加入 `--skip_final_save` 供诊断 run 使用。它解决了 smoke 结束阶段的问题，但正式长训的分布式 checkpoint 方案仍需优化。

## 6. 为什么速度极慢

现场 H200 遥测：

- 每卡显存约 83–143GB / 143.8GB；
- 多数时间功耗只有 120–220W / 700W；
- 温度约 37–43℃；
- 无 power/thermal throttling，GPU 保持 P0；
- 常见一两张卡 SM=100%，其余卡 0–10%，随后互换。

这表明 GPU 没有持续执行大 GEMM，而是在小 kernel、显存分配、通信和 rank 等待之间切换。

### 6.1 强制 eager MoE

A13B 每层 64 experts、top-8。当前 Transformers 路径逐 expert 执行 Python 循环、`where`、两个小 linear 和 `index_add_`。

seq_len=128 时，每层每 rank 约有 `128×8=1024` 个 token-expert assignment，平均每 expert 只有约 16 行。H200 很难被这种大量小 GEMM 吃满，而且不同 rank 的路由负载不同，会产生 straggler。

脚本强制 eager，是因为当前 NVIDIA torch 2.8 的 `grouped_mm` 在动态 expert token 数不满足 16 对齐时触发 CUDA assert。

### 6.2 FSDP 通信全部专家

A13B 虽然 active parameters 约 13B，但截断模型总参数仍为 38–43B。`FULL_SHARD` 每层仍要 all-gather 全部 64 个专家，而不是只通信当前 token 命中的 8 个专家。

正确的 MoE 训练通常采用 expert parallel：每张卡长期持有部分专家，只在卡间交换 token。当前 FSDP 路径反复搬运整层全部专家权重，和 MoE 的稀疏计算结构不匹配。

### 6.3 额外倍率

- `grad_accumulation_steps=2`：一个日志 step 包含两个完整 microbatch；
- gradient checkpointing：backward 时重算 forward；
- 移除 `no_sync()` 后，每个 microbatch 都需 reduce-scatter；
- 第一次 optimizer step 后创建 Adam state，显存逼近上限并增加 allocator/`limit_all_gathers` 同步。

这解释了 fresh2/fresh4 都在 step1 后明显变慢。

## 7. 为什么初始 loss 在 85–104

### 7.1 不是已经观察到的训练发散

当前不能把 `85.92 → 90.20` 解释为“一步训练把模型训坏了”：

- loss forward/backward 完成后才设置 LR 并 optimizer.step；
- warmup 下 step0 LR=0；
- step2 loss 是第一笔非零更新之前计算的另一个 batch。

fresh4 也只跑到 step6，最后 fresh LR 仅 `3.33e-6`，仍处于 150-step warmup 很早阶段。

准确说法是：

> 模型初始化时 loss 就处在严重 off-manifold 状态；当前尚未跑足够多的有效更新，无法判断它最终会恢复还是发散。

### 7.2 A13B 中层 massive activation

A13B 的中间 residual stream 有明显 massive activation：

- layer 8–24 hidden absmax 约 137–165；
- RMS 只有约 0.1–0.17；
- 到 layer31 absmax 才降到约 16.5。

原来的上 19 层承担了把中间表示逐渐整理成 final norm/head 可读取表示的工作。

已有只读 probe：

| 路径 | 结果 |
|---|---:|
| 完整 A13B | NLL 2.1163，PPL 8.301 |
| front13 直接接 final norm/head | NLL 86.9391 |
| front13 + 原模型最后 2 层 | NLL 106.9928 |
| h13 继续经过原始剩余 19 层 | QCMem PPL 约 14–16 |
| keep13+fresh2 初始 batch | loss 85.9229 |

fresh2 的初始 loss 几乎等于 front13-direct，说明随机 fresh 层初始化时还没有完成表示转换。

### 7.3 fresh 初始化尺度不匹配

预训练模型实测：

- 原 layer12/13/14 q_proj std 约 0.0104–0.0106；
- layer13 input layernorm weight mean 约 0.0577；
- final model.norm weight mean约 1.9328。

fresh 初始化：

- q_proj / expert std约 0.020；
- RMSNorm weight=1。

因此 fresh 权重约大 2 倍，fresh norm 相比 layer13 约大 17 倍。它不是初始化代码漏执行，而是标准随机初始化与 A13B 中层预训练尺度不匹配。

fresh4 初始 loss 103.77 高于 fresh2 的 85.92，符合“随机层越多，初始化扰动越强”；但 fresh4 同时容量更大，不能用前 6 个 warmup batch 判断其长期上限。

### 7.4 fresh MoE router/expert 也是随机的

每个 fresh 层同时随机初始化 router 和 64 个 experts：

- router 初始分配不稳定；
- 每个 expert 每 batch 只看到少量 token；
- 不同 expert 学习速度不同；
- 不同 rank 的专家负载不均衡。

这使 A13B 的 fresh MoE 层比 Qwen3 的 fresh dense 层更难恢复。

## 8. 正式长训前仍需解决的问题

1. **MoE 性能路径**：实现按 expert token 数补齐到 16 倍数的 grouped GEMM，或切换到支持 expert parallel / tensor parallel 的 Megatron、DeepSpeed-MoE、DeepEP/DeepGEMM 类实现。
2. **FSDP 不适配 MoE**：避免每层在所有卡上汇聚全部专家。
3. **初始化尺度**：fresh 层应参考原 layer13 附近权重和 norm 尺度，而不是直接使用 std=0.02、norm=1。
4. **输出头 healing**：A13B 的 lm_head 当前按 inherited 参数使用低 LR；这可能让 readout mismatch 修复过慢，应做独立消融。
5. **weight decay 分组 bug**：FSDP wrap 后用参数 shard 的 `ndim` 判断 decay，原参数变成 1D shard，当前日志只有 `fresh_nodecay` / `inh_nodecay`；配置的 weight decay 0.1 实际没有施加。
6. **正式 checkpoint**：使用 sharded/distributed checkpoint，避免 full model + optimizer state 集中到 rank0。

## 9. 推荐验证顺序

### Gate A：性能正确性

使用相同 batch 做 3-step A/B profiler：

1. eager MoE 当前基线；
2. aligned/padded grouped GEMM；
3. 如可用，expert-parallel 实现。

记录：microstep/optimizer-step 时间、GPU 平均功耗、SM active、NCCL 时间、峰值显存。只有稳态速度进入合理区间，才继续质量实验。

### Gate B：初始化和读出

固定同一 eval batch，比较：

- front13-direct；
- keep13+fresh2；
- keep13+fresh4；
- 原始尺度初始化的 fresh2/4；
- lm_head 低 LR vs 高 LR；
- 每层 hidden RMS/absmax、logits absmax。

### Gate C：100 个真实 optimizer updates

在修复后的快速路径上，并排跑 fresh2/fresh4，固定 eval batch 测：

```text
update 0 / 1 / 2 / 5 / 10 / 25 / 50 / 100
```

同时记录 fresh layers、final norm、lm_head、inherited front 的分组 gradient norm。

判断标准：

- 若 loss 很快从约 90 降到 `<20`：说明主要是可恢复 cold start；
- 若 50–100 个真实更新后仍在 80+：说明当前截断/初始化/readout 设计不可 healing，不应投入 20k；
- 通过 100-step gate 后再启动 20k，并使用可恢复的 sharded checkpoint。

## 10. 当前结论

1. `j=13` 的语义缓存结论可信，但不能自动推出 `keep13+fresh2/4` 一定可训。
2. 模型 transplant 已验证精确，数据、tokenizer、label shift、RoPE 和 mask 不是当前首要嫌疑。
3. FSDP gradient-accumulation 死锁已修，训练 loop 能完成 optimizer step。
4. 123s/step 主要是 `eager 64-expert MoE + FULL_SHARD 全专家搬运 + checkpoint + gaccum2` 的系统问题，不是 H200 算力不足。
5. loss 85–104 是初始化时的结构性 off-manifold，不是已经证实的训练发散。
6. A13B 的主要质量风险是：用 2/4 个随机 MoE 层替代 19 个负责 residual-stream 整形的预训练上层。
7. 在性能、初始化、weight decay 和 checkpoint 修复前，不应直接启动 20k。

## 11. 关键文件

- `BUG.md`：GPU/FSDP 问题记录；
- `HARU.md`：A13B j-sweep、massive activation 与实验结论；
- `PLAN_minimal_arch_A13B.md`：原始实施计划；
- `scripts/train_hunyuan_a13b_probe2.py`：A13B keep+fresh trainer；
- `scripts/train_qwen3_arch_probe2.py`：Qwen3 对照 trainer；
- `scripts/train_qcmem_distill_hy3.py`：Hy3 QCMem LoRA trainer；
- `versions/v_minimal_arch_probe2.md`：Qwen3 keep12+fresh2 结果；
- `versions/v_qcmem_hy3_port.md`：Hy3 j=32 结果；
- `paper/sections/tab_hy3_distill.tex`：Hy3 4k LoRA 蒸馏结果；
- `logs/hunyuan_a13b_keep13_fresh2_gradaccum_fix_regression.log`：fresh2 修复回归；
- `logs/hunyuan_a13b_keep13_fresh4_diag100.log`：fresh4 暂停前 6 steps。
