{
  "id": "B01",
  "status": "backlog",
  "updated": "2026-08-08",
  "next_gate": "persist the actual bottleneck latent and run long-memory quality",
  "next_gate_executable_20260814": "Four-arm comparison at ONE scale, with the bottleneck latent ACTUALLY PERSISTED: (1) stock + Read-LoRA, (2) bottleneck only, (3) bottleneck + Read-LoRA, (4) bottleneck + Read-LoRA + Write-LoRA. Mandatory reported quantity: bytes/token of what is written to the store (not of the restored hidden). Gate is only meaningful AFTER the blocking dependency below is fixed, because today all four arms would write the same full-width h_j and the storage axis would be constant across arms.",
  "next_gate_original_preserved": "The pre-existing next_gate ('persist the actual bottleneck latent and run long-memory quality') is retained UNCHANGED and is NOT superseded. next_gate_executable_20260814 is its operationalisation: the same two requirements (persist the latent; measure long-memory quality), now with the arm set, the mandatory reported quantity (bytes/token of what is WRITTEN, not of the restored hidden), and the ordering constraint relative to blocking_dependency. No conflict between the two was found.",
  "gpu_cost_estimate": {
    "value": "~25-40 GPU-h for the four arms at 8B if the two existing 8B j=12/d512 CPT endpoints are reused; ~60-80 GPU-h if the CPT legs must be re-run at more than 2000 steps",
    "basis": [
      "8B funnel CPT measured: outputs/qwenbott_funnel_L12_d512 ran 2000 steps at 2.83 s/step on world_size=4 (logs/qwenbott_funnel.out) = 1.57 h wall = 6.3 GPU-h. Its matched stock-continued control outputs/qwenbott_baseline_L12 measured 2.82 s/step, same shape = 6.3 GPU-h. Both final.pt are on wzc1 (16.4 GB / 16.4 GB).",
      "Read-LoRA measured: outputs/qcmem_distill_qwen_j12_r32_4k ran 4000 steps on world_size=8 in 21.8 min wall (distill_args.json 00:04:02 -> final/ 00:25:50, logs/qcmem_distill_qwen_j12_r32_4k.log) = 2.9 GPU-h per adapter. Four arms need at most 3 adapter trainings.",
      "LoCoMo eval measured: one arm = 3 shards, 20:58:52 -> 21:16:43 = 17.9 min wall = 0.89 GPU-h (locomo_results/hcache_j12_*_chatFALSE file mtimes). Judge is an OpenAI API call, not GPU.",
      "Write-LoRA has NO measured rate in this repo, so the upper end of the range is an extrapolation from the Read-LoRA rate, not a measurement."
    ],
    "excluded_from_this_estimate": "RULER/LongEval/BABILong replication and any second scale. Those are generalisation, not this gate."
  },
  "kill_gate": {
    "source": "PROPOSAL.md 'Kill 条件', copied verbatim (3 conditions)",
    "conditions": [
      "低秩 latent 在 RULER/LongEval 上不保留精确 evidence",
      "fixed LM tax 在强模型上扩大而非缩小",
      "full-depth RAG 在同存储预算下严格支配"
    ],
    "note_on_condition_2": "The existing 1B->3B evidence points the RIGHT way (tax shrinks with scale), so this condition is currently NOT firing; but it has only been tested at 1B and 3B from-scratch, never at 8B. See established_measurements.lm_tax_ppl_pct."
  },
  "established_measurements": {
    "_provenance_note": "Every number below was re-derived from the named file in this session (2026-08-14), not copied from PROPOSAL.md.",
    "pca_dim99_1B": {
      "vanilla": 1825,
      "bottleneck_d512": 438,
      "source": "outputs/e2e_1b_deltanll/ctx2048_gpu0.json baseline.pca_dim99 / bottleneck.pca_dim99",
      "reproduced_at_other_ctx": "ctx3072 and ctx4096 give 1829/439 and 1829/439 -- stable, so the 1825/438 pair is not a ctx artefact"
    },
    "pca_dim99_3B": {
      "vanilla": 2790,
      "bottleneck_d512": 467,
      "source": "outputs/e2e_3b_deltanll/ctx2048_gpu0.json",
      "reproduced_at_other_ctx": "ctx3072 2797/467, ctx4096 2800/467"
    },
    "delta_nll_at_rank128": {
      "1B": {
        "vanilla": 0.0304,
        "bottleneck": 0.0022
      },
      "3B": {
        "vanilla": 0.0669,
        "bottleneck": 0.0135
      },
      "source": "same two json, by_rank['128'].dNLL",
      "reading": "the bottleneck arm is far more truncation-robust at rank128 at BOTH scales; at rank256 the bottleneck arm's dNLL is slightly NEGATIVE (-0.0028 / -0.0015), i.e. truncation is free"
    },
    "lm_tax_ppl_pct": {
      "1B_16k_steps_layer6": {
        "baseline_ppl": 25.28,
        "d1024": "+4.5%",
        "d512": "+5.9%",
        "d256": "+8.5%"
      },
      "3B_16k_steps_layer6": {
        "baseline_ppl": 27.44,
        "d512": "+4.7%",
        "d256": "+5.8%"
      },
      "source": "status/QCMEM_PAPER_DRAFT.md:223-224 = status/RUN_REGISTRY.md dim-sweep table; ckpts outputs/sembott_{1b,3b}_{base,d256,d512,d1024}_16k/final.pt on wzc1",
      "arithmetic_rechecked": "26.42/25.28=+4.51%, 26.78=+5.93%, 27.42=+8.46%, 28.73/27.44=+4.70%, 29.04=+5.83% -- all match",
      "IMPORTANT_scale_mismatch": "PROPOSAL.md says '固定 LM tax 约 4-8.5%'. That range is TRAINING PPL tax from the 16k-step from-scratch dim sweep. It is NOT the same quantity as the r_max NLL gap in outputs/e2e_*_deltanll/ (which is +0.16%/-0.19%/-0.13% at 1B and +0.73%/+0.82%/+0.84% at 3B). Do not mix the two when writing.",
      "monotone_depth_tax": "a separate layer sweep at fixed d512 gives L1 +4.2% / L3 +5.8% / L6 +6.0% / L9 +9.5% / L12 +9.6% (status/RUN_REGISTRY.md) -- tax rises monotonically with bottleneck depth; see memory/bottleneck-layer-sweep-monotone"
    },
    "assets_on_wzc1_for_the_next_gate": {
      "8B_funnel_j12_d512": "outputs/qwenbott_funnel_L12_d512/final.pt (16,390,019,143 B) + arch_meta.json (bottleneck_layer 12, bottleneck_dim 512, unfreeze_from 12, n_params 8.195B)",
      "8B_matched_stock_control": "outputs/qwenbott_baseline_L12/final.pt (16,381,629,495 B)",
      "both_trained_on": "data/pg19_train.jsonl, 2000 steps, eff_bs 64, seq_len 2048, lr 1e-4 (train_args.json)",
      "NEVER_EVALUATED": "grep over the whole repo for 'qwenbott_funnel_L12_d512' returns 0 hits outside the launch script -- these two 8B endpoints have no eval results anywhere. That makes an 8B leg CHEAPER than PROPOSAL.md implies, and it is the first thing to check before budgeting new CPT.",
      "eval_support_exists": "scripts/eval_qcmem_locomo.py --bottleneck_ckpt already loads a funnel-Qwen arm (lines 1023-1051) and requires arch_meta.json next to the ckpt, which is present"
    }
  },
  "blocking_dependency": {
    "statement": "The store still holds the RESTORED full-width hidden, not the d_bottle latent, so the storage-saving claim is currently unmeasurable end-to-end.",
    "mechanism_verified_in_code": "scripts/train_qwen_bottleneck_continued.py:113-133 inject_bottleneck wraps layer j in BottleneckLayer (down->GELU->up, NO residual). The wrapper's OUTPUT is back at hidden_size, and QCMemModel caches that layer output (h_j). So the funnel constrains the RANK of what is stored but not the WIDTH of the bytes stored.",
    "consequence": "bytes/token is identical between the bottleneck and vanilla arms today. Until a d_bottle-width persist path exists, arms 2/3/4 of the next_gate differ only in quality, not in storage, and the proposal's headline ('可压缩的可持久化 latent') cannot be demonstrated -- only its precondition (low rank) can.",
    "from_proposal": "PROPOSAL.md '当前缺口' item 1 states this; recorded here so the gate is not launched before it is fixed"
  },
  "other_gaps_from_proposal": [
    "no complete quality/storage/latency frontier on a natural long-memory task",
    "never combined with Read-LoRA + Write-LoRA jointly",
    "strong-model / A13B leg blocked by implementation, init and MoE system issues"
  ],
  "novelty_checked": true,
  "novelty_status_detail": "RESOLVED 2026-08-15: RELATED_WORK.md written (see related_work_status). Supersedes the previous text, which read 'RELATED_WORK.md does NOT exist in this directory' and cited README's gate as blocking GPU. The novelty gate no longer blocks GPU; blocking_dependency (d_bottle persist path) still does, and that is a code defect rather than a paperwork one.",
  "updated_20260814": "2026-08-14 -- STATUS made decidable (next_gate made executable; kill_gate, gpu_cost_estimate, established_measurements, blocking_dependency, novelty_checked added). No scientific conclusion changed; all numbers re-derived from disk. 0 GPU.",
  "related_work_status": {
    "verdict": "audited",
    "file": "RELATED_WORK.md",
    "written": "2026-08-15",
    "gpu_spent": "0 -- CPU + proxied literature search only",
    "closes": "proposal/shared/literature/RELATED_WORK_GAP_AUDIT_20260808.md:91 rating 不足 (B01 was priority 4 of 7). All five named families covered, PLUS a sixth the audit MISSED (architectural latent KV: MLA/YOCO/CLA/TPA/CARE) which is B01's largest attribution risk. 15-item must-NOT-claim list + one-sentence safe gap.",
    "novelty_verdict": "NOT PREEMPTED, but B01's FRAMING and its CONSTRUCTION are each independently owned by a CONCURRENT paper, and the general 'pretrain a low-dimensional cached latent' idea is owned outright by MLA/DeepSeek-V2 (2024). The surviving claim is narrower than PROPOSAL.md and rests on two MECHANICAL distinctions: a residual-FREE width constraint on the RESIDUAL STREAM (not per-head KV), and a COMPRESSOR-FREE rank measurement (dim99 / dNLL-vs-rank).",
    "closest_prior_art": [
      "Training Transformers for KV Cache Compressibility (KV-CAT, arXiv:2605.05971, 2026-05-07; ICML 2026 Workshop HiLD POSTER -- OpenReview note FcCMc3s0TL, venueid ICML.cc/2026/Workshop/HiLD, NO Camera_Ready_Revision; workshop NOT main conference) -- OWNS B01's FRAMING: formalises KV compressibility as a property of the LEARNED REPRESENTATIONS, PROVES a compressible/non-compressible separation, and gives KV-CAT continued pretraining. CONCURRENT (~3 months, edge of window). Surviving differences: slot axis vs feature axis; stochastic masking vs hard residual-free width; KV tensor vs residual-stream h_j; compressor-mediated tradeoff curves vs an INTRINSIC compressor-free rank number.",
      "Variable-Width Transformers (arXiv:2606.18246, 2026-06-16, arXiv-only; DBLP CoRR 2026, OpenReview no match) -- OWNS B01's CONSTRUCTION: x-shaped narrow-middle LM trained FROM SCRATCH, BEATS parameter-matched uniform baselines on LM loss, -22% FLOPs, -15% KV memory/IO, and explicitly reports qualitatively different RESIDUAL-STREAM representations. CONCURRENT (<2 months). Key differentiator: its bottleneck is residual-PRESERVING inside a standard block; B01's BottleneckLayer is residual-FREE (verified in scripts/train_qwen_bottleneck_continued.py:112-133), which is the mechanical reason the taxes have OPPOSITE SIGN (VWT better than baseline; B01 +4.5-8.5% worse). NOTE: this residual-preserving reading is INFERRED from the abstract and NOT confirmed from VWT's architecture section -- if it is residual-free too, B01's sharpest differentiator weakens.",
      "MatryoshkaKV (ICLR 2025 Poster, OpenReview venueid ICLR.cc/2025/Conference + Camera_Ready_Revision; DBLP conf/iclr/LinZXKH0ZD25) -- OWNS B01's AXIS: explicitly targets the FEATURE-DIMENSION axis of the cache and trains nested/truncatable orthogonal projections with a Matryoshka strategy. Differs by starting from a PRETRAINED model and learning a projection ON TOP OF frozen representations. Truncatability itself is MRL's (NeurIPS 2022, DBLP conf/nips/KusupatiBRWSRHC22 -- resolved via DBLP because OpenReview title search returned only noise).",
      "DeepSeek-V2 / Multi-head Latent Attention (arXiv:2405.04434, arXiv-only -- CoRR only, startling for its influence and the venue call most worth re-checking) -- OWNS the general idea outright: a production LM whose CACHED object is a shared low-rank latent BY ARCHITECTURE AND PRETRAINING, ~81% KV reduction, 2 years earlier. Surviving difference: MLA compresses per-head K/V consumed WITHIN the same forward pass; B01 constrains the RESIDUAL-STREAM h_j handed ACROSS QUERIES to a frozen upper stack.",
      "CARE (ICLR 2026 Poster, OpenReview venueid ICLR.cc/2026/Conference + Camera_Ready_Revision) -- OWNS non-uniform PER-LAYER low-rank budget allocation aligned to ACTIVATIONS not weights. PyramidKV (COLM 2025 -- NOTE it also carries ICLR.cc/2025/Conference/Rejected_Submission; cite COLM only) owns the post-hoc layer-wise budget. Native Sparse Attention (ACL 2025, Anthology 2025.acl-long.1126) owns the 'train the property in, don't bolt it on' meta-argument in the sparsity axis."
    ],
    "must_not_claim_count": 15,
    "safe_gap": "A residual-FREE feature-axis bottleneck trained into the residual stream at one designated depth makes that layer's output INTRINSICALLY low-dimensional -- measurable as PCA dim99 and as dNLL under plain rank truncation, WITH NO COMPRESSOR IN THE LOOP -- at an LM tax that does not grow with model scale; and a memory that persists d_bottle-WIDTH latents (not restored full-width hiddens) then retains exact evidence on retrieval-closed long-context tasks at a bytes/token no post-hoc codec on a stock model attains at equal quality.",
    "new_requirement_surfaced": "A residual-FREE vs residual-PRESERVING ablation at matched (j,d). B01 does NOT have this arm, and without it Variable-Width Transformers is hard to distinguish from B01's construction. This is a NEW gate item produced by the literature pass.",
    "recommended_next_spend_is_NOT_the_four_arm_gate": "Two items dominate on information-per-GPU-hour: (1) fix the d_bottle persist path (0 GPU code work) -- until then arms 2/3/4 differ ONLY in quality and the storage headline is untestable, so the 25-40 GPU-h would buy the wrong axis; (2) evaluate the two EXISTING, NEVER-EVALUATED 8B endpoints (outputs/qwenbott_funnel_L12_d512 vs outputs/qwenbott_baseline_L12) -- the only kill-gate condition (#2, LM tax vs scale) answerable from assets already on disk.",
    "honest_gaps": "Semantic Scholar HTTP 429 on every call -> every venue call is SINGLE-AUTHORITY. arXiv API 429 for the first ~15 min; several searches returned implausible zero-hits and MUST be re-run. DBLP /search/publ/api intermittently 500/503. 10 entries are arXiv-only = venue UNVERIFIED FROM THIS NODE, not unpublished (incl MLA itself). ONE FAMILY NOT VERIFIED AT ALL: discrete latent codecs (VQ-VAE and successors) -- a B01 submission must close this. Abstracts read for KV-CAT/VWT/Through-the-Bottleneck/MatryoshkaKV/CARE/SeDeM/RAC only. Cross-disk NOT checked: all B01 assets recorded as wzc1; ls-confirm before dispatching to .73/.82/.104. Full list in RELATED_WORK.md section 5.",
    "promotion": "NOT YET -- the kill gate has not run, and blocking_dependency (the d_bottle persist path) is a VERIFIED CODE DEFECT, not a literature issue. Re-verified from source this session: scripts/train_qwen_bottleneck_continued.py:112-133."
  },
  "blocking_dependency_discharged_20260817": {
    "supersedes_key": "blocking_dependency",
    "supersedes_paths_verbatim": [
      "blocking_dependency.statement",
      "blocking_dependency.mechanism_verified_in_code",
      "blocking_dependency.consequence",
      "blocking_dependency.from_proposal"
    ],
    "discharges": [
      "blocking_dependency.statement",
      "blocking_dependency.mechanism_verified_in_code",
      "blocking_dependency.consequence",
      "blocking_dependency.from_proposal"
    ],
    "verdict": "DISCHARGED. The d_bottle-width persist path now exists, is exercised end-to-end, and its equivalence + storage saving are MEASURED on the real 8B endpoint. The four blocker clauses above are kept VERBATIM (append-only, LIFECYCLE_SCHEMA.md sec 0); they were accurate when written and describe the code as of 4e96d42.",
    "discharged_by_commit": "58ff2d2",
    "commit_message_is_WRONG_read_this": "The code landed inside 58ff2d2, whose subject is 'chore(status): topology correction -- our 5 nodes ARE taiji pods'. A CONCURRENT `git commit` from another agent picked up the staged index before this change could take its own commit. Content at 58ff2d2 is correct and complete; history was NOT rewritten because other agents were committing to main at the time. A follow-up EMPTY commit 6260eab documents this so a bisect does not chase a subject line that never mentions B01.",
    "what_changed": {
      "src/memory/qcmem/qcmem_model.py": "New `persist_bottleneck_latent` ctor flag (DEFAULT FALSE). `_write_band` runs layers[0:j-1] + the funnel's wrapped inner layer, then returns `funnel.act(funnel.down(h))` -- the d_bottle-wide latent -- instead of the funnel's restored full-width output. `_lift_latent` re-applies the deferred bias-free `up` to each cached piece on the READ side (per piece, BEFORE the pack, so the GEMM shapes match the legacy run). `_resolve_deferred_funnel` validates placement; `store_bytes_per_token()` reports the width actually persisted.",
      "scripts/semantic_bottleneck_model.py": "Docstring only: records the three properties of BottleneckLayer that make the split legal (bias-free linear `up` applied last; elementwise GELU BEFORE `up`; no residual and nothing between `up` and the return), so a future funnel redesign must revisit it.",
      "scripts/eval_qcmem_locomo.py": "New `--persist_bottleneck_latent` with five fail-early guards, and it prints store_bytes_per_token into every arm's log.",
      "scripts/qcmem_bottleneck_persist_selftest.py": "NEW. CPU/CUDA gate.",
      "scripts/qcmem_bottleneck_persist_8b_smoke.py": "NEW. 8B real-checkpoint gate."
    },
    "why_it_is_exact_not_approximate": "The write band computes h_j = up(act(down(g(x)))) and the read band computes f(h_j). Persisting s = act(down(g(x))) and reading f(up(s)) is the SAME composition -- only the cut point moves. `act` (GELU) STAYS on the write side because it is the nonlinearity; `down` cannot be deferred (it IS the width reduction); `up` cannot be deferred unless the funnel is the LAST layer of the write band, which is why resume_j == bottleneck_layer + 1 is enforced rather than recommended.",
    "measured_equivalence_8B_real_endpoint": {
      "ckpt": "outputs/qwenbott_funnel_L12_d512/final.pt (16,390,019,143 B, step=2000)",
      "config": "resume_j=13 (== bottleneck_layer 12 + 1), bf16, 1x B200 (LOCAL), sdpa, 3x256-tok context chunks + 64-tok query + BOS sink, 3 decode steps",
      "max_abs_diff": 0.0,
      "differing_elements": 0,
      "n_logits_compared": 128385920,
      "per_tensor": "read 0/126,562,688 | read_tail 0/1,215,488 | decode 0/607,744",
      "greedy_tokens": "[782, 2090, 288, 11] on BOTH paths",
      "verdict": "BIT-IDENTICAL on the real 8B weights. This is a measurement, not an inference from the algebra: max |diff| printed as 0.000000e+00 with an element-wise inequality count of 0.",
      "evidence": "proposal/backlog/B01-semantic-bottleneck-memory-ready-models/evidence/persist_8b_smoke_20260817.json"
    },
    "measured_bytes_per_token_the_gates_mandatory_quantity": {
      "definition_used": "numel * element_size() / T of the tensor WRITE actually returns -- i.e. of what is written to the store, NOT of the restored hidden. This is the distinction next_gate_executable_20260814 calls mandatory.",
      "legacy_full_width": "8192 B/tok  (shape [1, 64, 4096], bf16)",
      "persist_d_bottle": "1024 B/tok  (shape [1, 64, 512],  bf16)",
      "ratio_measured": 8.0,
      "ratio_expected_hidden_over_dbottle": 8.0,
      "whole_context_store": "a 3x256-token context went from 6,291,456 B to 786,432 B",
      "note": "The storage axis is therefore NO LONGER CONSTANT across the four arms; arms 2/3/4 now differ from arm 1 in storage as well as quality, which is what the gate needs.",
      "evidence": "proposal/backlog/B01-semantic-bottleneck-memory-ready-models/evidence/persist_8b_smoke_20260817.json"
    },
    "measured_legacy_regression_protects_published_numbers": {
      "method": "The gate materialises `git show HEAD:src/memory/qcmem/qcmem_model.py` into a separate module and runs BOTH implementations over the same weights -- so the baseline is the file as COMMITTED, not a hand-written reimplementation of what I believe the old code did.",
      "coverage": "9 observable tensors per arm (write_sink, write_q, write_ctx_cat, write_chunks_cat, write_prefill_hj, read, read_tail, resume_forward_ids, decode) x {bottleneck arm, bottleneck_dim=0 VANILLA arm} x {CPU, CUDA} x {fp32, bf16}",
      "result": "max_abs 0.0, 0 differing elements in every cell. The flag defaults OFF and the default path is byte-identical.",
      "why_the_vanilla_arm_matters": "bottleneck_dim=0 is the configuration every already-published QCMem number in this repo was produced under. It is checked explicitly rather than assumed to be covered.",
      "evidence": [
        "proposal/backlog/B01-semantic-bottleneck-memory-ready-models/evidence/persist_selftest_cpu_20260817.json",
        "proposal/backlog/B01-semantic-bottleneck-memory-ready-models/evidence/persist_selftest_cuda_20260817.json"
      ]
    },
    "negative_controls_all_verified_to_fire": [
      "QCMemModel refuses persist on a backbone with no funnel at layers[resume_j-1] (would silently persist full width)",
      "refuses resume_j == bottleneck_layer (funnel not last in band)",
      "refuses resume_j > bottleneck_layer+1 (band would consume full width before the store)",
      "refuses resume_j == 0 (empty write band)",
      "a persist-path read REJECTS a full-width cached piece instead of packing it",
      "eval_qcmem_locomo.py errors on --persist_bottleneck_latent without --bottleneck_ckpt (rc=2, verified)",
      "errors with --baseline hcache/kvdirect/cacheblend (rc=2, verified)",
      "errors with --top_prepay_b != 0 (rc=2, verified)",
      "errors on the STOCK-CONTINUED ckpt outputs/qwenbott_baseline_L12 (arch_meta bottleneck_dim=0) (rc=2, verified against the real arch_meta)",
      "errors when --resume_j != bottleneck_layer+1 (rc=2, verified against the real arch_meta)"
    ],
    "reproduce": [
      "CPU:  /opt/conda/envs/torch-base/bin/python scripts/qcmem_bottleneck_persist_selftest.py --json_out <f> > <log>; echo $?   # 34 PASS / 0 FAIL",
      "CUDA: CUDA_VISIBLE_DEVICES=0 ... scripts/qcmem_bottleneck_persist_selftest.py --device cuda ...                            # 34 PASS / 0 FAIL",
      "8B:   CUDA_VISIBLE_DEVICES=0 ... scripts/qcmem_bottleneck_persist_8b_smoke.py --json_out <f> > <log>; echo $?             # 6 PASS / 0 FAIL",
      "NOTE the `cmd > file; echo $?` form: piping into tail would report the PIPE's rc, not the gate's."
    ],
    "gpu_spent": "~5 minutes on ONE B200 (LOCAL) for the 8B smoke (40.5 s model load + <2 s of forwards) plus the CUDA fixture gate. The rest was 0 GPU.",
    "what_is_NOT_discharged_by_this": "Only the STORAGE-MECHANISM blocker is closed. The four-arm QUALITY comparison has not been run, the ~25-40 GPU-h estimate stands, and other_gaps_from_proposal (no full quality/storage/latency frontier; Read-LoRA + Write-LoRA never combined; the strong-model/A13B leg) are untouched. The residual-FREE vs residual-PRESERVING ablation that related_work_status.new_requirement_surfaced added is also still open and is the arm that separates B01 from Variable-Width Transformers."
  },
  "defect_found_while_verifying_20260817": {
    "claim_in_repo": "src/memory/qcmem/qcmem_model.py write_chunks docstring asserted the batched writer is 'bit-identical' to per-chunk write_chunk.",
    "status": "TOO STRONG whenever a funnel BottleneckLayer sits in the write band. PRE-EXISTING -- reproduced against git HEAD with this change's files absent, so it is NOT caused by the persist path, and it affects the legacy full-width path identically.",
    "diagnosis": "A batched-vs-unbatched GEMM kernel/blocking difference. The funnel's down/up are the only plain nn.Linear layers in the write band, which is exactly why the divergence appears only when the funnel is present. The docstring's SEMANTIC claim (no cross-chunk information flow; attention never mixes batch entries) is INTACT and is what its argument actually establishes; only the stronger BITWISE wording was wrong.",
    "action_taken": "The write_chunks docstring now records the counterexample with its numbers instead of repeating the claim, and the gate asserts 'persist path no worse than legacy' rather than an absolute bit-identity it cannot have. Recorded here because 'bit-identical' is the kind of claim other gates get built on.",
    "evidence": [
      "proposal/backlog/B01-semantic-bottleneck-memory-ready-models/evidence/preexisting_writechunks_gemm_HEADonly_20260817.txt",
      "proposal/backlog/B01-semantic-bottleneck-memory-ready-models/evidence/preexisting_writechunks_gemm_cpu_HEADonly_20260817.txt"
    ],
    "measured_source_a_pre_existence_proof_git_HEAD_only": {
      "_note": "HEAD code only: BOTH qcmem_model.py and semantic_bottleneck_model.py loaded via `git show`, so the persist path takes no part. Each row has a funnel-REMOVED control at IDENTICAL shapes -- that control is what makes the attribution stick.",
      "CUDA bf16, hidden 4096 / d_bottle 512, funnel in band": "3764/393216 elements differ, max_abs 1.5625e-02",
      "CUDA bf16, IDENTICAL shapes, funnel REMOVED": "0/393216 -- bit-identical",
      "CPU fp32, hidden 256 / d_bottle 32, funnel in band": "11271/16384 differ, max_abs 6.9849e-09",
      "CPU fp32, IDENTICAL shapes, funnel REMOVED": "0/16384 -- bit-identical",
      "bare nn.Linear(4096,512,bias=False) CUDA bf16, B=2 vs two B=1": "82/49152 differ, max_abs 3.9062e-03",
      "bare nn.Linear(256,32) CPU fp32, B=2 vs two B=1": "1376/1536 differ, max_abs 6.1095e-07; bit-identical in bf16"
    },
    "measured_source_b_same_phenomenon_as_the_gate_sees_it": {
      "_note": "The LEGACY arm of check 5 in scripts/qcmem_bottleneck_persist_selftest.py. Different seeds and chunk mix than source (a), so the counts differ slightly; same phenomenon. Listed separately rather than merged, because quoting one source's count under the other's label is how a number stops being traceable.",
      "CPU fp32 tiny/float32": "11218/16384 differ, max_abs 5.588e-09",
      "CUDA bf16 real/bfloat16": "6422/393216 differ, max_abs 3.125e-02",
      "CPU real/bfloat16 and both tiny/bfloat16 cells": "0 differing -- bit-identical"
    },
    "_correction_note": "An earlier draft of THIS key (written the same session, never committed) quoted source (b)'s CPU fp32 count (11218 @ 5.588e-09) under source (a)'s git-HEAD-only label. The two are different runs. Both are now reported with their own provenance.",
    "measured_superseded_by_the_two_keys_above": {
      "CUDA bf16, hidden 4096 / d_bottle 512, funnel in band": "3764/393216 elements differ, max_abs 1.56e-02",
      "CUDA bf16, IDENTICAL shapes, funnel REMOVED": "0/393216 -- bit-identical",
      "CPU fp32, hidden 256 / d_bottle 32, funnel in band": "11218/16384 differ, max_abs 5.59e-09",
      "CPU fp32, IDENTICAL shapes, funnel REMOVED": "0/16384 -- bit-identical",
      "isolated to a bare op": "nn.Linear(4096, 512, bias=False) in CUDA bf16 at B=2 vs two B=1 calls: 82/49152 elements differ, max_abs 3.91e-03"
    }
  }
}
