
=== ready_cpu  (8) ===
  B03-cyclic-layer-reset-boundary
     why: DECLARED lifecycle=ready_cpu in STATUS.json (authoritative; Both remaining blockers are 0 GPU, so LIFECYCLE_SCHEMA.md 1.1 puts this in ready_cpu rather than parking it: (1) the read-out/decision rule for the 1B core gate does not exist (see next_gate), (2) novelty is unadjudicated and RELATED_WORK.md is absent. NOT cl… ⟨TAIL RETRACTS, spliced⟩ APPENDED 2026-08-15, NOT rewritten: blocker (2) IS NOW DISCHARGED -- RELATED_WORK.md exists and novelty_checked is true (see related_work_status). Blocker (1), the missing read-out/decision rule, is STILL LIVE and is the sole remaining prerequisite; it is 0 GPU. lifecycle stays ready_cpu and next_gate stays NOT_SPECIFIED. The pre-append sentence is kept verbatim so the record shows what was true …)
     gate[next_gate_executable_20260814]: {"_key_name_note": "The DATE IN THIS KEY NAME IS THE READER… ⟨TAIL RETRACTS, spliced⟩ Writing into the slot the code actually reads is therefore correct, and NOT editing ready_queue.py for one low-priority backlog item is deliberate. AUTHORED 2026-08-15; see .written below.", "written": "2026-08-15", "gpu_spent_designing": 0, "supersedes": "the sentinel string in the pre-existing `next_gate` key (still on disk, byte-unchanged per LIFECYCLE_SCHEMA.md sec 0), which reads 'NOT_SPECIF…
     cost: UNKNOWN -- 需先做 1-cell 计时
     blocker[gpu_policy]: NO GPU FOR B03. This blocker is STILL BLOCKING and remains outstanding. Writing the read-out (which is what next_gate_executable_20260814 and kill_gate_executable_20260814 now do,…
  B04-eval-fragility-incubator
     why: DECLARED lifecycle=ready_cpu in STATUS.json (authoritative; 3/3 adversarial lenses returned NEEDS_REVISION (wf_c2a3b490-e40, 2026-08-14 23:0x). The decidability lens found the gate NOT decidable: phi used rescale span 116500 while the read-out's own span is 175000, so the statistic as written was not the measured quan…)
     gate[next_gate]: {"id": "G1-budget-discrimination-at-fixed-damage", "stateme… ⟨TAIL RETRACTS, spliced⟩ The JSON side reproduces; PROPOSAL.md's table must be marked superseded BEFORE a threshold is quoted against it.", "decidable_outcome": "The decision statistic is phi = max( max(y)-min(y), |OLS slope of y on heal_step| * 175000 ) / 0.021820, where y = median_margin at heal steps 25000/50000/100000/128000/200000 and 175000 = 200000-25000 is that read-out's OWN span. PASS: phi <= 0.30, i.e. excursi…
     cost: 1.08 GPU-h (gate G1). G0 is 0 GPU-h. Downstream matched-family ladder 244-2560 GPU-h, NOT authorised until G1 passes.
  B05-semantic-handoff-phase-diagram
     why: DECLARED lifecycle=ready_cpu in STATUS.json (authoritative; UNCHANGED BY DESIGN. REV-1 (2026-08-15) applied the specific_fix of all three adversarial lenses (wf_c2a3b490-e40) plus one defect no lens raised, and the gate now has an executable adjudicator whose --selftest PASSES pre-data. That does NOT make it ready_gpu…)
     gate[next_gate_executable_20260814]: {"statement": "Run the NATIVE-SUFFIX READOUT COLUMN at j>0 -- the one column A02 never ran -- and pair it cell-for-cell against A02's on-disk distilled-LoRA arms at the same depths. 4 arms x 4 retrieval-closed RULER cel…
     cost: 3.34 GPU-h j-weighted point estimate; BUDGET 9.5 GPU-h with measured decode-cap headroom = ~25 min wall on one 8xH20 node; ZERO t…
  B06-portable-decompression-adapter
     why: DECLARED lifecycle=ready_cpu in STATUS.json (authoritative; Novelty gate adjudicated CLEARED 2026-08-17 (see novelty_verdict_20260817), so paperwork no longer blocks B06. It stays ready_cpu because its OWN next steps are 0 GPU and two of them can still NARROW or KILL the claim: (1) `wc -l` on the canonical judge cache…)
     gate[next_gate]: Same-harness rejudge of the canonical HCache predictions to… ⟨TAIL RETRACTS, spliced⟩ y published as 10.13 (status/PAPERA_RESULTS_CONSOLIDATED.md:175), and 10.13 is arithmetically consistent with 8.11 as the SAME run (uniquely: 156 of 1540 judge-correct + 5 of 446 regex-correct gives 8.1067 blended and 10.1299 on Judge_1:4), so the conversion may already be done -- verify that before spending anything. Then: BABILong/RULER/LongEval replication; a second residual/checkpoint compres…
     cost: {"drift_resolution_leg": "0 GPU-h. Two of the three relevant judge caches are on wzc1 with 1540 records each; a rejudge is an API…  (NO BASIS)
  B07-mutable-comem-serving
     why: DECLARED lifecycle=ready_cpu in STATUS.json (authoritative; STILL ready_cpu, unchanged; SUPERSEDES lifecycle_why_20260814, which is FACTUALLY FALSE and is NOT edited. There is NO hard-coding of B01/B05/B06/B07/B08 in ready_queue.py: grep -nE '"(B01|B05|B06|B07|B08)"' returns rc=1, 0 lines, and lines 46-51 are that fil… ⟨TAIL RETRACTS, spliced⟩ Writing it is 0 GPU; PROVING IT CORRECT IS NOT, so it cannot be closed on this node. (2) MUTATION LEG HAS NO APPARATUS -- blocking_dependency_20260815 stands unchanged and was re-verified this session: zero matches in src/ and scripts/ for content_hash|chunk_hash|store_version|version_id|generation_id|epoch_id, and the only def update in src/memory/qcmem/ is qcmem_model.py:1091, which is _CacheBl…)
     gate[next_gate_executable_20260817_a]: REV-1 binding. SUPERSEDES the STATISTIC of next_gate_executable_20260814 (not edited). PASS = Delta_8 of TTFT MEDIANS >= 123.2 ms, UNPAIRED bootstrap of the median difference, B=20000, seed 42, CI95 excluding 0, sign po…
     cost: UNKNOWN -- 需先做 1-cell 计时
     blocker[blocking_dependency]: {"statement": "Every mechanism in '关键设计' (versioning, content/compatibility hashes, fail-closed stale objects, overlap dependency graph, mixed-version fallback) is a system that d…
  B08-memory-applications
     why: novelty gate not adjudicated (novelty_verdict.verdict=NEEDS_NARROWING -- not preempted (nothing is 完全相同/抄袭; all four newly found hits… (PENDING)) -> the actionable task is 0 GPU: run it
     gate[next_gate]: {"statement": "On the RETRIEVAL-CLOSED LongMemEval-S stratum (question types with measured any_hit_recall == 1.000 at BM25 top_k=10: knowledge-update n=78 + single-session-assistant n=56, stratum n=134), hold retrieval …
     cost: ~1.15 GPU-h on ONE sm_90 card at n=134 (budget ceiling 2 GPU-h); 4.31 GPU-h if the K2 evaluability precondition escalates to n=50…
  B09-trajectory-aware-sft-data-selection
     why: DECLARED lifecycle=ready_cpu in STATUS.json (authoritative; Novelty is now adjudicated CLEARED (novelty_verdict_20260817), so PAPERWORK no longer blocks B09 -- but DATA still does, and the remaining work is 0 GPU. GATE -1 resolved on 2026-08-16 to 'a 1.0 GiB download + CPU derive task, NOT a rollout-generation project… ⟨TAIL RETRACTS, spliced⟩ - download the SWE-bench/SWE-smith-trajectories tool split (MIT, 8 parquet shards) to data/agent_traj/ on wzc1, ASSERT n_rows==24100 / resolved 9427-14673 / 9116 rows with both message_type and tool_calls rather than trusting the HF index, emit the flat pool with PROPOSAL section 1's columns, and PRE-REGISTER the section 4.3/H3 re-scope from benchmark-holdout to repo-family holdout BEFORE looking…)
     gate[next_gate]: ["GATE -1 (NEW 2026-08-10, BLOCKING, was never scheduled): … ⟨TAIL RETRACTS, spliced⟩ - KEPT VERBATIM; it is a good gate, it is simply downstream of GATE -1", "freeze task-family-level candidate/query/validation/test split", "implement constraint-matched stratified random, RDS+, and TA-Coreset selectors", "implement a row-multiplicity stress test", "add either joint hierarchical marginal gain or branch-verified decision credit", "run fixed-target-token three-seed validation pilot"…
     ! no gpu_cost_estimate
  B11-generative-scorer-format-fragility
     why: DECLARED lifecycle=ready_cpu in STATUS.json (authoritative; K1 (novelty, 0 GPU, blocking) is RUN and RELEASED for the narrowed claim, so gpu_policy no longer blocks. But the recommended next step is deliberately 0-GPU: file the unclaimed upstream bug report (K3 exit) for the metrics.py:31 dead-code guard, which is B11…)
     gate[next_gate]: K3-EXIT FIRST (0 GPU): re-check that booydar/babilong still… ⟨TAIL RETRACTS, spliced⟩ a bug (it kills correct list-format answers and rescues correct continuation-leak answers), with the executed two-row demonstration. ONLY IF a maintainer indicates the behaviour is intentional, escalate to K2 (cross-family replication, ~1.0 GPU-h for the 2 contrast arms over 6 cells) with per-arm LIST-FORMAT RATE as a pre-registered covariate. Otherwise B11's content is 'a scorer line is dead cod…
     cost: 0 GPU-h for the recommended next step (upstream bug report). K2, if it is ever escalated to: ~1.0 GPU-h for the 2 contrast arms o…
     blocker[gpu_policy]: NO GPU until K1 (novelty, 0 GPU) passes.

=== needs_prior_gate  (3) ===
  A02-comem-write-read-repair
     why: gate + kill gate + novelty all OK, but 2 un-discharged blocker(s) in its own record: gpu_policy = NO further A02 GPU. Resurrection requires a NEW MECHANISM, not another read-out of the same ladder. [VERBATIM copy of disposition_2026_08_12.gpu_policy, surfaced to the TOP level 2026-08-15. Reason, …; disposition_2026_08_12.gpu_policy = NO further A02 GPU. Resurrection requires a NEW MECHANISM, not another read-out of the same ladder.
     gate[next_gate]: ["A02 is NOT cleared for promotion. Storage form DEAD (2048… ⟨TAIL RETRACTS, spliced⟩ Both would benefit plain text-RAG identically, so neither supports a CoMem thesis.", "SUPERSEDED 2026-08-12 by matched_quality_depth_control (below): 'NOT tested anywhere yet: a matched-quality depth control (a LoRA distilled for j=0) -- without it, depth cannot be isolated from adapter quality. This is the only remaining way to make a clean depth claim, and it requires TRAINING, not eval.' The t…
     blocker[gpu_policy]: NO further A02 GPU. Resurrection requires a NEW MECHANISM, not another read-out of the same ladder. [VERBATIM copy of disposition_2026_08_12.gpu_policy, surfaced to the TOP level …
     ! no gpu_cost_estimate
     ! blocker STILL LIVE [gpu_policy]: NO further A02 GPU. Resurrection requires a NEW MECHANISM, not another read-out of the same ladder. [VERBATIM copy of disposition_2026_08_12.gpu_poli…
     ! blocker STILL LIVE [disposition_2026_08_12.gpu_policy]: NO further A02 GPU. Resurrection requires a NEW MECHANISM, not another read-out of the same ladder.
  A04-recovery-certification
     why: gate + kill gate + novelty all OK, but 1 un-discharged blocker(s) in its own record: blocked_by.still_blocking_before_any_gate_gpu[2] = USER APPROVAL for GPU. The full gate is 1,077-4,309 GPU-h; nothing beyond Pilot Zero may be launched without it.
     gate[next_gate]: ["PILOT ZERO (CPU + <=3 GPU-h, run FIRST): commit T=2.0%/5k and rho=0.85 to git BEFORE analysis, then use the checkpoints already on disk to ask whether a PLATEAU-accepts / NI-rejects disagreement exists at all. Can fir…
     cost: {"measured_anchors": {"1B_8xH20": "median 2.02 s/step (n=36, logs/olmo2_1B_keep7fresh2_1node.log, world_size=8 bs=16 gaccum=1 eff…  (NO BASIS)
     blocker[blocked_by]: {"related_work_gate": "CLEARED 2026-08-09 (see related_work_status). NO LONGER BLOCKING.", "still_blocking_before_any_gate_gpu": ["PROPOSAL.md rewrite to the narrowed safe_residua…
     ! blocker STILL LIVE [blocked_by.still_blocking_before_any_gate_gpu[2]]: USER APPROVAL for GPU. The full gate is 1,077-4,309 GPU-h; nothing beyond Pilot Zero may be launched without it.
  B01-semantic-bottleneck-memory-ready-models
     why: gate + kill gate + novelty all OK, but 4 un-discharged blocker(s) in its own record: blocking_dependency.statement = The store still holds the RESTORED full-width hidden, not the d_bottle latent, so the storage-saving claim is currently unmeasurable end-to-end.; blocking_dependency.mechanism_verified_in_code = scripts/train_qwen_bottleneck_continued.py:113-133 inject_bottleneck wraps layer j in BottleneckLayer (down->GELU->up, NO residual). The wrapper's OUTPUT is back at hidden_size, and QCMemModel caches…; blocking_dependency.consequence = bytes/token is identical between the bottleneck and vanilla arms today. Until a d_bottle-width persist path exists, arms 2/3/4 of the nex ...
     gate[next_gate_executable_20260814]: Four-arm comparison at ONE scale, with the bottleneck latent ACTUALLY PERSISTED: (1) stock + Read-LoRA, (2) bottleneck only, (3) bottleneck + Read-LoRA, (4) bottleneck + Read-LoRA + Write-LoRA. Mandatory reported quanti…
     cost: ~25-40 GPU-h for the four arms at 8B if the two existing 8B j=12/d512 CPT endpoints are reused; ~60-80 GPU-h if the CPT legs must…
     blocker[blocking_dependency]: {"statement": "The store still holds the RESTORED full-width hidden, not the d_bottle latent, so the storage-saving claim is currently unmeasurable end-to-end.", "mechanism_verifi…
     ! blocker STILL LIVE [blocking_dependency.statement]: The store still holds the RESTORED full-width hidden, not the d_bottle latent, so the storage-saving claim is currently unmeasurable end-to-end.
     ! blocker STILL LIVE [blocking_dependency.mechanism_verified_in_code]: scripts/train_qwen_bottleneck_continued.py:113-133 inject_bottleneck wraps layer j in BottleneckLayer (down->GELU->up, NO residual). The wrapper's OU…
     ! blocker STILL LIVE [blocking_dependency.consequence]: bytes/token is identical between the bottleneck and vanilla arms today. Until a d_bottle-width persist path exists, arms 2/3/4 of the next_gate diffe…
     ! blocker STILL LIVE [blocking_dependency.from_proposal]: PROPOSAL.md '当前缺口' item 1 states this; recorded here so the gate is not launched before it is fixed

=== promoted  (1) ===
  A01-null-calibration-methodology
     why: status/promoted_to says promoted
     gate[next_gate]: ["gate-1 depth-curve gap-fill: COMPLETE (all four families … ⟨TAIL RETRACTS, spliced⟩ general scope: COMPLETE 2026-08-09 (retraction history preserved as a table; mechanism explicitly scoped family-specific; Qwen3 transition contradiction resolved)", "novelty check (third kill clause): COMPLETE 2026-08-09, DOES NOT FIRE -- see novelty_check_2026_08_09 and NOVELTY_CHECK.md", "REMAINING before promotion to paper<X>: (a) full-text pass on Balepur et al. ACL 2024 and Oostermeijer ICML…
     ! no kill_gate field (README: kill gate before GPU)
     ! no gpu_cost_estimate

=== dead  (3) ===
  B02-adaptive-depth-and-read-budget
     why: DECLARED lifecycle=dead in STATUS.json (authoritative; Own pre-registered kill gate FIRED on both lengths, both clauses. Not killed by prior art; killed by our own negative measurement. See KILL_GATE_VERDICT.md.)
     gate[next_gate]: RE-RUN the j-sweep on a FIXED sample set, then measure per-example oracle headroom
     cost: 6.44 GPU-h for the full confirmatory sweep (16 cells = 8 resume_j x {16k,32k}, n=200); ALREADY SPENT, 0 remaining
     blocker[required_before_stage0]: ["fix n (sample count) and a per-config RNG seed protocol so the same items are scored under every resume_j", "re-run the j-sweep with that fixed set (1 GPU x 8 configs x n items)…
  B10-dllm-infilling-ar-dominance
     why: DECLARED lifecycle=dead in STATUS.json (authoritative; Own pre-registered kill gate FIRED (gate_1, base-axis re-score, 2026-08-15: exact McNemar p=1.0000, b=39 c=38, |delta|=0.00096805 < 0.02; both KILL conditions hold, thresholds unmodified). Not killed…)
     gate[next_gate]: REWRITE B10 AS A PROTOCOL NOTE, OR ARCHIVE IT. 0 GPU. This is the verbatim if_killed action of kill_gate.gate_1, which FIRED on 2026-08-15 (see gate_1_result and GATE1_BASE_AXIS_VERDICT.md). Concretely: (a) decide proto…
     cost: {"slate_estimate": "24 GPU-h, 'marginal cost is prose'", "audited_estimate": "~4-8 GPU-h, and the FIRST step is 0 GPU", "why_slat…  (NO BASIS)
  B12-slorb-rank-efficiency
     why: DECLARED lifecycle=dead in STATUS.json (authoritative; SUPERSEDES lifecycle_20260817 ONLY IN ITS VOCABULARY, not in its content. That key is left byte-unchanged per LIFECYCLE_SCHEMA.md sec 0 (append-only); it says 'killed_by_own_gate', which is NOT one o…)
     gate[next_gate]: G0 (0 GPU, BLOCKING, two legs, do BOTH before requesting a card). LEG 1 -- novelty: write proposal/backlog/B12-slorb-rank-efficiency/RELATED_WORK.md clearing surfaces (a)-(d) in novelty_status_detail; the load-bearing q…
     cost: 1.46 GPU-h to a go/no-go verdict (pilot pair: rung P + control Dctl). 4.38 GPU-h for the full 6-cell ladder (A, P, Q, R, S, Dctl)…
     blocker[gpu_policy]: NO GPU until G0 (next_gate) passes -- both legs, novelty write-up AND the CPU rung-A export self-check. G0 is 0 GPU and is the entire actionable next step. When G0 passes, the aut…

SUMMARY: 0 ready_gpu, 8 ready_cpu (0 GPU, dispatchable NOW), 3 needs_prior_gate
  => An idle GPU is NOT a reason to idle: there are 8 zero-GPU tasks that are blocking their own gates.

  ⚠ 1 STALE ABSENCE ASSERTION(S): some STATUS.json still says a file is missing that is ON DISK.
    Before you dispatch an agent to CREATE any file, run: python3 proposal/check_stale_absence_claims.py
    (measured 2026-08-17: relaying one of these cost an agent most of its run disproving the premise)
