# Historical Adaptation-CKA Evidence

These probes were computed on checkpoints associated with the abandoned Paper C v1
SQuAD setup. They document the checkpoints faithfully but **must not** be used as
evidence for a general adaptation-onset depth claim.

The aggregate `all_curves.json` was deleted because it duplicated the seven
per-experiment JSON files while omitting `tag` and `base_meta`.

