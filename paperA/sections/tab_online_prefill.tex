% Select-first online-prefill cohort at 8k--128k.
% Kept in the text flow so it cannot float above the appendix heading.
\begin{center}
\captionsetup{hypcap=false}
\centering
\small
\setlength{\tabcolsep}{4.5pt}
\begin{tabular}{@{}lrrrrr@{}}
\toprule
\textbf{Length} & \shortstack{\textbf{Dense}\\\textbf{prefill}} & \shortstack{\textbf{CoMem}\\\textbf{prefill}} & \textbf{Speedup} & \shortstack{\textbf{Dense}\\\textbf{peak}} & \shortstack{\textbf{CoMem}\\\textbf{peak}} \\
\midrule
8k   & 1.10\,s  & 0.94\,s & 1.17$\times$ & 18.8\,GB & 18.6\,GB \\
32k  & 5.94\,s  & 1.09\,s & 5.45$\times$ & 25.0\,GB & 18.7\,GB \\
128k & 50.59\,s & 1.32\,s & 38.3$\times$ & 50.0\,GB & 18.7\,GB \\
\bottomrule
\end{tabular}
\captionof{table}{\textbf{Store-ready online-prefill operating points.}
Measurements use one H20, Qwen3-8B, bf16/SDPA, and the median of three runs
after one warmup. Dense is the stock, LoRA-off Qwen3 path and processes the
complete source; CoMem reads a fixed
6,657-token top-12 pack with its rank-32 LoRA. The persistent store is already
built, so these values isolate the store-ready online path and exclude one-time
Write and external-store fetch. The matched incremental depth effect is the
separate $1.403\times$ result in Table~\ref{tab:core-tradeoff}; the
Write-inclusive same-adapter L20A cohort appears in Table~\ref{tab:eff}.
The 50.59\,s dense row is therefore a stock-dense master reference, not a
same-adapter control.}
\label{tab:online-prefill}
\end{center}
