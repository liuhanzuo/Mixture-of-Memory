# Ablation: 无 memory，仅骨干模型
# 用于与有 memory 的方案做对比
defaults:
  - ../base

memory:
  num_memories: 0               # 关闭 memory

fusion:
  fusion_type: "none"           # 不使用 fusion

loss:
  utility_weight: 0.0           # 不需要 utility 损失
